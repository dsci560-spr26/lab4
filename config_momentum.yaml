# Momentum Rebalance Strategy Config
# Usage: python backtester.py config_momentum.yaml

strategy:
  module: Strategy.momentum_rebalance
  class: MomentumRebalanceStrategy
  params:
    invest_amount: 100000
    top_n: 50
    lookback_days: 200
    rebalance_frequency: monthly

start_date: "2015-01-01"
end_date: "2025-12-31"
starting_cash: 100000

output_dir: result/MomentumRebalance_10Y
